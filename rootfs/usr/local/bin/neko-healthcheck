#!/bin/sh -eu

: "${NEKO_SERVER_BIND:=0.0.0.0:8080}"
port="${NEKO_SERVER_BIND#*:}"
wget -q -O - "http://127.0.0.1:${port}/health" >/dev/null 2>&1 \
|| wget --no-check-certificate -q -O - "https://127.0.0.1:${port}/health" >/dev/null 2>&1 \
|| exit 1
